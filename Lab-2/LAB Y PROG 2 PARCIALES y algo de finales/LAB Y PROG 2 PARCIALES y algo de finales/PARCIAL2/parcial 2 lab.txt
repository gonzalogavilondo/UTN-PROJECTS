typedef struct
{
    int idSucursal;
    char articulo[30];
    int stock;
    float precio;
}registroArchivo;

typedef struct nodoArticulo
{
    char articulo[30];
    int stock;
    float precio;
    struct nodoArticulo* sig;
}nodoArticulo;

typedef struct
{
    int idSucursal;
    nodoArticulo* articulos;
    struct nodoSucursal* sig;

}nodoSucursal;

1.cargar los datos desde los archivos
	a. Leer del archivo de articulos cada registro de una lista simplemente enlazada de sucursales, donde la insercion es ordenada por 
Id sucursal creciente. Cada nodo de la lista de sucursales posee una sublista de todos los articulos  que tienen o han tenido
en la sucursal(puede haber stock 0).

2. Hacer una funcion que informe el id de sucursal que posee el mayor VALOR TOTAL en stock de articulos
(Es la suma de cada valor de cada articulo en stock: precio * stock).

3. Hacer una funcion que elimine de la lista de sucursales todos aquellos articulos que posean un stock de 0.

4. Hacer una funcion que informe la sucursal con la menor cantidad de articulos en stock